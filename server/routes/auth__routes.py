"""
auth_routes.py

Handles authentication related API endpoints.

Responsibilities:
- Register new users
- Login existing users

Example endpoints:
POST /register
POST /login

These endpoints interact with the Users table in the database.
"""