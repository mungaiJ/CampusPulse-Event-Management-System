"""
routes package

This folder contains all API routes for the application.

Routes define the endpoints that the frontend can call.

Examples:
- /register
- /login
- /events
- /events/{id}/register

Each route file handles a specific part of the system.
"""